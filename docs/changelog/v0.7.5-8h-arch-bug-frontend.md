# v0.7.5 — 8h 用户改动整合 (架构 + bug 修复 + frontend 整理 + 文档)

## 背景

8h 用户 review (biomatrix) + 真用发现: dream 跑一周后, 用户从产品视角 + 算法工程师视角
写下 [IMPROVEMENT_PLAN.md](../../IMPROVEMENT_PLAN.md) (100 行) + [ALGORITHM_REVIEW.md](../../ALGORITHM_REVIEW.md) (108 行). 同时改了 27 个文件 (1005+/379-), 包含架构重做 + 3 真 bug 修复 + 9 frontend 文件整理.

v0.7.5 把这 8h 改动从 uncommit 转成正式 git 历史 (4 commits), 可追溯.

## 4 commits

### 1. refactor(arch): GlobalOptions 5 层 config 合并 + RuntimeContext 打包 + 隐私守门
- GlobalOptions 加 RuntimeContext struct (resolved + provider + budget + dreamConfig 打包)
- 加 RuntimeConfigError.cloudProviderRequiresConsent (openai-compat 隐私守门)
- 加 resolvedRuntimeConfig(writeDefaultVaultConfig:) throws 5 层合并:
  CLI > vaultConfig > settings > env > hardcoded
  (vaultConfig 真正读 .dream/config.json, 走 DreamConfigLoader.load)
- 加 runtimeContext(writeDefaultVaultConfig:) async throws 工厂, 包 BudgetedLLMProvider
- ResolvedDreamRuntimeConfig 加 toConsolidationConfig() / toDecayConfig() 桥接
- settings.llmChoice 三个 case (.mock / .ollama / .openaiCompat) 都覆盖
- CLI.cmdRun 改用 try await opts.runtimeContext()
- DecayConfig 加 Sendable

### 2. fix: ForceLayout 边去重笔误 + NightlyDreamScheduler 错误捕获 + RawReadonlyGuard 真实修复

**fix 1: ForceLayout 边去重笔误**
- 老代码 `"\(e.u)|\(e.u)"` 两侧都用 e.u, 永远不重复检测
- 修: normalizedUniqueEdges helper, min/max 排序, Set<String> 去重
- 影响: P2-2 图谱 ForceLayout 跑出更稳定位置

**fix 2: NightlyDreamScheduler launchctl 错误捕获**
- 老代码 try? p.run() + 忽略 exit code, 失败 silent
- 修: LaunchctlResult (exitCode + output + userMessage), 捕获 stdout/stderr
- Status.enabled 改为 plistExists && launchdLoaded
- Status.lastError 区分 'No such process' 跟真错误
- plist 存在但 launchd 没加载 → Status.lastError 自动报告

**fix 3: RawReadonlyGuard 真实修复 (P0-3 后续, 关键 bug)**
- 老 readonlyDirMode = 0o555 把 raw/ 目录也设只读, **GUI Import 锁死** (无 owner 写权限)
- 用户报告拖拽入 vault 卡死
- 修: 文件 0o555 (不可写), 目录 0o755 (owner 可加文件)
- 加 isProtectedRawFile (md + txt)
- 加 makeFileReadonly(_:) 公开 helper (Import 落盘后立刻压回只读)
- isReadonly 改用 enumerator 递归扫
- chmod0o555 → chmodReadonlyMode (语义化)

### 3. test: 配套测试调整 + 新增 7 文件
- ForceLayoutTests (+15): 验 normalizedUniqueEdges
- RawImporterTests (+46): 验跟新 makeFileReadonly 协调
- RawReadonlyGuardTests (27 行): 验新 readonlyDirMode=0o755 + makeFileReadonly
- RuntimeConfigAndBudgetTests (+41): 验 resolvedRuntimeConfig + RuntimeContext + 隐私守门
- WikiKindPersisterTests (+2)
- EditorStateTests (新 +25)
- LaunchCorrectnessTests (+10)
- MenuBarControllerTests (+8)

### 4. feat(frontend+docs): Sources/dream/ 9 文件 + FrontendPresentation + 13 changelogs + verify 脚本
- 9 frontend files 整理 (跟 GlobalOptions.llmProvider() → runtimeContext() 协调)
- FrontendPresentation 新增 (82 行): 纯 UI formatting helpers
  (vaultDisplayName / budgetUsage / monthlySpend / documentTitle / relativePath / documentKindLabel)
- FrontendPresentationTests (127 行): 配套测试
- 13 changelogs 串起 v0.5.0-rc3 到 v0.6.1 完整轨迹
- IMPROVEMENT_PLAN.md (100 行) + ALGORITHM_REVIEW.md (108 行)
- scripts/verify-p9-p9b.sh 端到端验证脚本

## 数字

- 27 files modified + 17 untracked
- +1005 / -379 lines
- 4 commits (arch / fix / test / frontend+docs)
- 623/623 tests pass (老 616 + 7 user 新增)
- 0 退化

## Tags

- v0.7.5: 8h 改动整合收口
- v0.7.6: 真 Ollama 量化 (P3-8 §3 真价值)
- v0.8.0: 整 v0.7 系列 release notes (含真量化报告)

## 后续 (v0.8+)

- 真跑 Ollama 量化更多模型 (qwen2.5:3b / llama3.1:8b)
- 细看 错 case (8 verify 错 + 2 contradiction 错)
- CI smoke + nightly eval (Makefile eval-ollama-{verify,contradiction} 拆 phase 跑)
- .harness/ 搭建 (给项目搭 agent team)
