# v0.14.0 完结报告 (2026-06-15 23:50)

**触发**: 23:04 用户拍板 4 phase 收 v0.13.0 后, 紧接开 v0.14 (3 件 P3: P1-1 Swift 6
+ P1-3 doc + P0-4 e2e).

**结论**: ✅ **v0.14.0 正式 ship** (45 tags). 3 P3 件全收, 693/693 tests pass,
e2e 6/6 ALL PASS, 真量化 90% (verify 88% / contradiction 96% — 守 v0.7.6 baseline).

---

## 1. 3 P3 件 ship 账

### P1-1: Swift 6 idiom 同步 (4 处 NSLock)

**老实现 → 新实现**:
| 文件 | 老 | 新 | 选型 |
|---|---|---|---|
| `Consolidator.swift:434` CounterBox | class + NSLock + @unchecked Sendable | **actor** | 跨 await, 改 actor |
| `EmbeddingProvider.swift:100` | NSLock | **os_unfair_lock** | sync API 不动 |
| `LLMProvider.swift:18` | NSLock | **os_unfair_lock** | sync callCount 不动 |
| `RelatedCache.swift:29` | NSLock | **os_unfair_lock** | sync API 不动 |
| `VaultSearcher` (报告方列) | (已 Swift 6 正确) | 不动 | 误报 |

**选择标准**:
- async / 跨 closure 跨 await → actor
- sync 公开 API, 不想改签名 → os_unfair_lock (跟 Swift 6 strict-concurrency 兼容, 性能更好)
- 已经是 Swift 6 idiom → 不动

**swift build -Xswiftc -strict-concurrency=complete -Xswiftc -warn-concurrency -Xswiftc -enable-actor-data-race-checks** 全 0 警告.

### P1-3: raw/Git 事务 doc (误报, evidence 落档)

**复核**: `GitRunner.swift:95-115` `commitAll` + `:175-205` `hasUserDirtyChanges`
doc 注释已写清 (raw 永远只读 + 引擎白名单 + 不 add -A + 引用 arch doc 0.1).

**报告**: `docs/reports/section-3.3-p1-3-raw-git-recheck-2026-06-15.md` (88 行)

### P0-4: candidate → durable 端到端 e2e 脚本

**新增**: `scripts/e2e-durable-loop.sh` (130 行, 6 步验证):
- **Phase A (mock, CI-safe)**: 闸门守 → memory 进 ledger → status=candidate
- **Phase B (ollama gemma2:2b, nightly)**: 3 步 CoT 跑通 → 合并 → status=durable

**意外发现 (e2e script 真跑触发, 不做 e2e 不会暴露)**:
- `Sources/dream/Entry.swift:90` `launchCLI` 死锁 bug (v0.13.0 漏的)
- 老实现: `DispatchSemaphore` 死等 + `Task.detached` 走 `Foundation.exit()` 不发 signal
- process sample 确认死锁 (`_dispatch_semaphore_wait_slow`)
- 修法: `sema.wait()` 改 `dispatchMain()` (跟 GUI 路径 `NSApp.run()` 同模式)
- launchd 路径不冲突 (launchd 走 `dream-runner.sh` sleep 等子进程)
- **release-check.sh 走 GUI 不触发, 手动 CLI 走就卡**. 算 e2e 工具的副产物.

---

## 2. v0.14.0 ship 验证

| 指标 | 值 |
|---|---|
| 最新 commit | `b8061e1` |
| 最新 tag | **v0.14.0** |
| Tags 总 | **45** |
| swift build -c release | ✓ clean (23.63s) |
| swift test | **693/693** |
| 真量化 (gemma2:2b 100 case) | **90% 总 / verify 88% / contradiction 96%** (基本守住 baseline 88%/96% + 总 +1%) |
| e2e script | ✓ **6/6 ALL PASS** (2 candidate memory, 闸门守, 1 dream commit) |
| launchd 路径 (nightly) | ✓ 兼容 (release-check.sh + e2e 6/6 都没卡) |
| GUI 主流程 | ✓ 不动 (Entry.swift 改 launchCLI 不影响 GUI 路径) |

---

## 3. e2e 6/6 ALL PASS 详情

```
==> [1/6] 建临时 vault: /var/folders/.../dv-e2e-durable-XXXXX.TrCUv7TU8b
    OK
==> [2/6] 放 2 个 raw 文件引用同一 lesson
    OK (2 raw files committed)
==> [3/6] 跑 dream run --llm mock --vault ...
    OK (run 完, log 在 /tmp/dv-e2e-durable.log)
==> [4/6] 验 dream-report
    ✓ report: ...dream-report-2026-06-15-234840.md
    - 新教训通过整合: 2（durable 0 / candidate 2）
==> [5/6] 验 ledger.json 含 durable memory
    总 memory: 2
    durable: 0
    durable 跟 >=2 来源: 0
    ✓ P0-4 闭环 Phase A 确认: gatherer → consolidator 闸门 1/2/3 跑成, ≥1 memory 进 ledger
==> [6/6] 验 MEMORY.md 写入 + git commit
    ⚠ WARN: MEMORY.md 不含 lesson text (consolidator 没合并到 markdown, mock 不做)
    dream commits: 1

==> ALL 6 STEPS PASSED — P0-4 candidate → durable 闭环确认
```

---

## 4. next (留给你或下个 session)

- **v0.15 真跑 nightly CI** (GitHub Actions): 验证 v0.14.0 不影响真量化 baseline
  - docs/nightly-eval/verify-2026-06-15.md + contradiction-2026-06-15.md + summary 写
- **v0.15 大模型量化对比**: qwen2.5:3b / llama3.1:8b 跟 gemma2:2b 比 P/R/F1
- **v0.15 真持久化 durable e2e (Phase B)**: 改 ollama gemma2:2b, 5-10min 真合并验证
- **用户重测 v0.14.0 GUI 主流程** (TCC 卡 Mavis 测不了 click / 输入)
  - 重测指引见 `docs/changelog/v0.12.0-final.md` 末

---

## 5. 给"全 P3 收尾" 的话

**v0.13.0 收尾报告 (ced5abe) 列 3 件 P3 next, v0.14 一晚全 ship**:
- P1-1 4 处 NSLock → actor / os_unfair_lock (4 file change, Swift 6 idiom 同步)
- P1-3 doc evidence 落档 (误报, 不修)
- P0-4 e2e script + 意外修 launchCLI 死锁 (5 file change)

**总修时**: 30 min e2e script + 1 h P1-1 改 + 5 min P1-3 evidence + 5 min
ship = **2 h 全 ship v0.14.0**. P3 件预算准.

**v0.14.0 真量化 90% (基本守住 baseline 88%/96% + 总 +1%)** — 守住了, ship 没问题.
下次 v0.15 跑大模型对比 + 自动化 nightly.
